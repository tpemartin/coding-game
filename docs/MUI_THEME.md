# MUI Theme

## Location

Theme config is in `src/theme.ts` and applied in `src/main.tsx`.

## Customize

Edit palette, typography, and component overrides in `createTheme`.

Example:

```ts
const theme = createTheme({
  palette: {
    primary: { main: '#0ea5e9' },
  },
})
```
